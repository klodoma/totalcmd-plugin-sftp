---
description: Save or update this branch's session-context file and commit it
---

Run `~SC` as `.agents/commands.md` defines it.

Only in repos that have a `session-context/` folder -- auto-trader today. If this repo has
none, say so and stop rather than creating the convention here by accident.

Write or update `session-context/<branch-name>.md` for the branch currently checked out,
matching the branch name exactly, slashes and all. Cover the headings in
`session-context/README.md`, quote the request in the asker's own words rather than
paraphrasing it, and say plainly what is unverified. Then commit it on that branch.
